# Which Magento store each hostname runs. A hostname not listed runs the store's default, and kapelos adopt --store writes a site's own map.
map $host $kapelos_run_code {
    default "";
}

map $host $kapelos_run_type {
    default "";
}
